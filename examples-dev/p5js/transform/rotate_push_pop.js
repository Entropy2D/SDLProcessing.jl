function setup() {
  let a;                 // Angle of rotation
  let offset;  // Angle offset between boxes
  let num;            // Number of boxes

  function setup() {
    createCanvas(640, 360, WEBGL);
    noStroke();
    
    a = 0.;
    offset = PI / 24.0;
    num = 12;
}