function setup() {
  var bx;
  var by;
  var boxSize = 75;
  var overBox = false;
  var locked = false;
  var xOffset = 0.0;
  var yOffset = 0.0;

  function setup() {
    createCanvas(640, 360);
    bx = width / 2.0;
    by = height / 2.0;
    rectMode(RADIUS);
}